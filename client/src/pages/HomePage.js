import React from "react";
import { Col, Row, Container } from "../components/Grid";

const Home = () => {
  return (
    <Container fluid>
        {/* module in here */}
    </Container>
  );
};

export default Home;