import React from "react";
import { Col, Row, Container } from "../components/Grid";

const CompleteSale = () => {
  return (
    <Container fluid>
        {/* module in here */}
    </Container>
  );
};

export default CompleteSale;